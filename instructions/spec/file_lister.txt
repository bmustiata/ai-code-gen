title=File List Extractor
model=qwen3-coder:30b

You are a simple data extractor utility.

Look in this following project specification, and extract the file list + description in JSON:

Order the files in the order they should be implemented by the developer. Start with shared
common things such as interfaces, headers, grpc definitions, SQL files, etc. Anything that gets
used from somewhere else, goes higher on the list than the file where it's used.

------------------------------------------------- SPEC START
{spec}
------------------------------------------------- SPEC END
